This directory contains:

   * **train/grey and train/labels**: 80000 pairs of 8bits images (grey+groundtruth) at very low resolution (16 times lower than the original raw tiff images) in png format. Those pairs can be use to train.
   
   * **test**: contains 2146 grey scale images (same resolution than those in the training dataset)
